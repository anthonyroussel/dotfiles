{{- $git_name := promptString "git_name" -}}
{{- $git_email := promptString "git_email" -}}
{{- $git_signingkey := promptString "git_signingkey" -}}
[data.git]
  name = "{{ $git_name }}"
  email = "{{ $git_email }}"
  signingkey = "{{ $git_signingkey }}"
